Create a new React component in this project.

## Instructions

1. Create the component file at `app/components/$ARGUMENTS.tsx`
2. Use TypeScript with proper prop types
3. Apply Tailwind CSS for styling
4. Follow the existing code style in the project

If a design asset is referenced, look for it in `.claude/assets/designs/`.
