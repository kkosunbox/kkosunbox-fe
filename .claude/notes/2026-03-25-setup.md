# 작업 현황 요약 — 2026-03-25

## 완료된 작업

### 1. Claude Code 프로젝트 구조 세팅
`.claude/` 하위 디렉토리를 Best Practice 기준으로 구성

```
.claude/
├── settings.local.json
├── commands/
│   ├── create-component.md   # /create-component 슬래시 커맨드
│   └── review.md             # /review 슬래시 커맨드
├── contexts/
│   ├── fonts.md              # 폰트 목록 및 추가 방법
│   └── design-system.md      # 컬러 시스템 + 스타일링 규칙
├── assets/
│   ├── designs/              # 피그마 스크린샷 저장 경로
│   └── screenshots/          # 기타 참조 스크린샷
└── notes/
    └── 2026-03-25-setup.md   # 이 파일
```

### 2. CLAUDE.md 생성
- 위치: 프로젝트 루트 `CLAUDE.md`
- 내용: pnpm 명령어, 라우트 구조, 폰트 로딩 방식, 스타일링 설정

### 3. 컬러 시스템 구축 (시맨틱 토큰 10개)
- 레퍼런스: `.claude/assets/screenshots/image-001.png`
- 구현: `app/globals.css` `:root` + `@theme inline`

#### 최종 확정 토큰

| 그룹 | Token | HEX |
|---|---|---|
| Brand | `--color-primary` | `#C97A3D` |
| Brand | `--color-secondary` | `#F6E9DD` |
| Brand | `--color-accent` | `#7FB3FF` |
| Surface | `--color-background` | `#FFF9F3` |
| Tier | `--color-premium` | `#F07F3D` |
| Tier | `--color-plus` | `#3E9ED9` |
| Tier | `--color-basic` | `#3F6900` |
| Text | `--color-text` | `#2F2F2F` |
| Text | `--color-text-secondary` | `#999999` |
| Text | `--color-text-muted` | `#DDDDDD` |

### 4. 타이포그래피 시스템 구축 (21개 클래스)
- 레퍼런스: `.claude/assets/screenshots/image-002.png`
- 구현: `app/globals.css` `@layer components`
- 문서: `.claude/contexts/typography.md`

네이밍 규칙: `text-{role}-{size}-{weight}` (예: `text-title-42-b`)

| 그룹 | 클래스 수 |
|---|---|
| Titles | 4 |
| Subtitles | 7 |
| Body | 4 |
| Captions | 2 |
| Action | 3 |
| **합계** | **20** |

### 5. 메인 페이지 초안 (app/page.tsx)
- 레퍼런스: `.claude/assets/screenshots/image-003.png`, `.claude/assets/designs/figma-css.txt`
- `layout.tsx`에 Google Fonts 추가: `Ms_Madi`, `Give_You_Glory` (CSS 변수: `--font-ms-madi`, `--font-give-you-glory`)

**섹션 구성 (위→아래):**
| 섹션 | 배경 | 주요 내용 |
|---|---|---|
| GNB | white | 로고, 메뉴, CTA 버튼 |
| Hero | 오렌지 그라디언트 (#F9D6B5→#ECA265) | 헤드라인, 서브텍스트, CTA, 제품 이미지, "very Good!" 데코 |
| Stats Bar | #171713 | 선택하기 / 이용하기 / 다양하게 맞보기 |
| Benefits | #FFF9F3 | 브랜드 텍스트, 해시태그, 강아지 원형 사진 |
| Package Plans | #F6E9DD | 3-column 패키지 카드 (Basic/Standard/Premium) |
| Ingredients | white | Point 2, 재료 텍스트, CTA, 재료 이미지 |
| Pain Points | #FFF8F2 | "for you" 데코, 고민 불릿 리스트 |
| Footer | #FFF9F3 | 로고, 태그라인, 4개 feature 아이콘 |

**이미지 플레이스홀더:** 모든 실제 이미지는 placeholder 상태 (bg 컬러로 대체)

### 6. 라우트 추가 및 FSD 아키텍처 도입
- 추가 라우트: `/about`, `/subscribe`, `/support`, `/login` (모두 "개발 중" 상태)
- `components/` 폴더 제거 → FSD 구조로 전환
- `UnderConstruction` → `shared/ui/UnderConstruction.tsx` 이동
- `shared/ui/index.ts` public API 패턴 적용
- FSD 레이어 디렉토리 생성: `widgets/`, `features/`, `entities/`, `shared/`
- 아키텍처 문서 추가: `.claude/contexts/architecture.md`

## 다음 작업 후보
- [ ] 실제 이미지 에셋 교체 (패키지, 강아지 사진, 재료 이미지)
- [ ] 모바일 반응형 대응
- [ ] GNB를 `widgets/header/`로 분리
- [ ] 패키지 카드를 `entities/package/`로 분리
