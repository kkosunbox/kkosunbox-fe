import { PasswordManagementSection } from "@/widgets/mypage";

export const metadata = { title: "비밀번호 변경 | 꼬순박스" };

export default function PasswordPage() {
  return <PasswordManagementSection />;
}
