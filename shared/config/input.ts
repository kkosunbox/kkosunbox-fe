/**
 * Standard height for `<input>`, input-like controls (DatePicker trigger, BreedCombobox),
 * and gender toggles shown beside inputs.
 * Use the Tailwind class `h-10` (40px) in className strings — do not interpolate dynamically (JIT).
 */
export const TEXT_INPUT_HEIGHT_PX = 40;

/** Documented alias — write `h-10` literally in JSX/CSS classes. */
export const TEXT_INPUT_HEIGHT_CLASS = "h-10" as const;
